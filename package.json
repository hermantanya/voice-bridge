{
  "name": "voice-bridge",
  "version": "1.1.0",
  "description": "Real-time voice translation for two people. English, Hebrew, and Russian. Push-to-talk, phone + browser.",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix server && npm run build --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
